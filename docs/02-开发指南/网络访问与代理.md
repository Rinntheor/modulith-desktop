# 网络访问与下载源

本文说明应用的两条联网链路（插件市场、软件更新）在哪里发起、用户配置的「下载源」如何生效，以及如何诊断连不上。

## 1. 谁在联网

**前端不发起任何网络请求。** `src/` 下没有 `fetch`、没有 `XMLHttpRequest`、没有指向远程主机的 `<img src>` 或 `<script src>`。所有与网络有关的动作都通过 `invoke` 交给后端，唯一的例外是 `openUrl()`（交给系统浏览器）。

后端只有一个 `reqwest` 出站出口，外加一个不由我们控制的更新器传输：

| 执行者 | 代码位置 | 负责 |
| --- | --- | --- |
| `NetClient`（**唯一出站门面**） | `src-tauri/src/modules/net/client.rs` | 判定策略、写流量日志、真正发送。裸 `reqwest::Client` 由它私有持有，不对外提供 |
| ↳ 插件管理 | `plugins/manager.rs` | 插件索引、索引签名、README、市场图标、`.lcp` 包下载，以及插件的 `network` 请求 |
| ↳ 网络诊断 | `settings/probe.rs` | 「设置 → 网络 → 网络诊断」的逐条实测 |
| `tauri-plugin-updater` 的 `Updater` | `updater/commands.rs` | 更新清单与安装包下载。**它用自己的传输，不经过门面** —— 见 §8.5 |
| CSP `connect-src` + 前端门面 | `tauri.conf.json` / `src/services/netGuard.ts` | **WebView 里的直接出站**。插件联网应当走 `ctx.http`；直接发请求由 CSP 挡死、由门面解释原因 —— 见 §8.6 |

前端的地址模板集中在 `src/config/pluginRegistry.ts` 一个文件里；后端从不构造地址，只校验宿主。

## 2. 两种联网方式

设置项在「设置 → 网络」：

| 字段 | 取值 | 默认 |
| --- | --- | --- |
| `networkMode` | `direct` / `proxy` | `direct` |
| `githubProxy` | 代理根地址，例如 `https://gh-proxy.org` | 空串 |

**两者同时满足才真的走下载源。** 只判模式会让「选了下载源但地址是空的」退化成把一个绝对地址拼到空串上；只判地址会让「切回直连」失效。判断收敛在一处：后端 `settings/network.rs` 的 `proxy_base()`，前端镜像为 `utils/networkSettings.ts` 的 `isProxyEffective()`。

默认直连是刻意的：一个默认打开的第三方加速源会把「装什么插件」交给一个我们无法控制的中间人。

## 3. 下载源的语义：前缀拼接

这类服务（gh-proxy 系）的用法是把原始地址**整条接在自己的域名后面**：

```
https://gh-proxy.org/https://github.com/Rinntheor/modulith-desktop/releases/latest/download/latest.json
└──── 代理根 ────┘└──────────────────────── 原始地址 ────────────────────────────┘
```

拼接由 `settings/network.rs` 的 `join_proxy()` 完成，尾部斜杠可有可无（`https://gh-proxy.org` 与 `https://gh-proxy.org/` 结果相同）。

**只有 GitHub 宿主的地址会被改写**（`GITHUB_HOSTS`：`github.com`、`raw.githubusercontent.com`、`objects.githubusercontent.com`、`codeload.github.com`、`api.github.com`、`gist.github.com`、`release-assets.githubusercontent.com`，以及 `*.githubusercontent.com` 后缀）。`cdn.jsdelivr.net` 不在其中 —— 它是一个独立的 CDN 而不是 GitHub 的镜像，把它的地址拼到 GitHub 加速源上只会换来 404。

## 4. 改写在哪里发生，以及为什么必须在那里

改写**只在后端**发生，而且发生在**地址校验之后**：

```
前端给出原始地址
  → ensure_registry_url_allowed(url)     校验宿主 ∈ ALLOWED_REGISTRY_HOSTS
  → network::rewrite_url(url, proxy)     拼上用户配置的下载源
  → reqwest 发请求
```

这样安排有一个直接的安全后果：**宿主白名单始终作用在我们自己写死的宿主上**。用户填的代理地址从来不作为参数进入校验，因此一个被改坏的代理字段最多让请求失败，不可能把请求导向一个原本不被允许的宿主。若让前端拼好地址再传进来，就等于让被约束的一方自己划定边界 —— 那正是 `manager.rs` 里那条注释在防的事。

同样地，「网络诊断」的探测目标列表里，插件仓库那两条来自前端（因为地址模板在前端），更新清单那几条由后端从 `tauri.conf.json` 追加；两者都要通过同一套宿主校验。

## 5. 应用更新为什么搬进了后端

历史上这一步由前端直接调用 `@tauri-apps/plugin-updater`。它的两处地址都不可配：

1. **清单地址**来自 `tauri.conf.json` 的 `plugins.updater.endpoints`，在插件初始化时固定。
2. **安装包地址**根本不在插件配置里，而是**清单文件自己写的**（`platforms.<target>.url`，指向 `github.com/.../releases/download/...`）。

因此「能检查到新版本，下载却卡住」是这种结构的必然结果：清单可以换个地址取，安装包不行。

现在两步都走 `src-tauri/src/modules/updater/commands.rs`：清单地址经 `UpdaterBuilder::endpoints()` 传入，安装包地址在 `check()` 之后改写 `update.download_url`（该字段是公开的）。下载、签名校验、调起安装程序仍然全部由官方插件完成，公钥仍然取自 `tauri.conf.json` 的同一个 `plugins.updater` 块。

配套的两个决定：

- `src-tauri/capabilities/default.json` 里**移除了 `updater:default`**。插件与应用共用同一个 JS 上下文，若官方插件的命令仍然可达，插件代码就能绕开下载源直接调用它 —— 那样这个设置项就成了「只对守规矩的调用者生效」。
- 前端不再 import `@tauri-apps/plugin-updater`（依赖暂时保留在 `package.json` 里，因为改依赖清单需要重新生成锁文件；见第 8 节）。

## 6. 失败时的行为

**下载源失败会回退直连。** 顺序是「先下载源、后直连」：用户配它就是为了优先用它；而回退保证一个填错或临时挂掉的加速源不会把插件市场与应用更新变成永久不可用。两条路都失败时，日志里会有两次尝试的完整原因。

**候选顺序会随设置变化。** 前端 `pluginRegistry.ts` 的 `registrySources()` / `pluginIndexSources()` 接受一个 `preference`：选了下载源时把 GitHub 那条排到 CDN 前面。理由是受限网络里直连那一步要先等一个连接超时（30 秒）才轮到加速源，那会让「我明明配了下载源，市场还是打不开」成为一个必然的抱怨。谁先谁后不影响装到什么 —— 两条候选指向同一个不可变 tag，包还有索引里的 sha256 兜底。

**地址允许明文 http 的唯一情形是本机回环地址**（`localhost` / `127.0.0.1` / `::1`），用于本地自建加速源调试。注意应用更新比这更严：官方更新器在 release 版拒绝非 https 的清单地址，因此 http 回环加速源只对插件市场有效。

## 7. 诊断

「设置 → 网络 → 网络诊断」逐条实测并把结果并排显示：每条给出 HTTP 状态、响应字节数、耗时与展开后的失败原因（`network::describe_error_chain()` 会把 `reqwest` 藏在 `source()` 里的真正原因 —— DNS 解析失败、证书不受信、连接被重置 —— 一并列出来）。

探测并发执行（`tauri::async_runtime::spawn`），每条 8 秒超时。原因是串行时最坏情况是「条数 × 超时」，用户会以为界面卡死。

失败原因的展开也应用在正常请求上：`PluginError` 的 `From<reqwest::Error>` 会保留整条因果链，因此界面上的「网络错误」不再只有一句 `error sending request for url (...)`。

## 8. 出站管控与流量日志

「设置 → 网络」里的出站策略、离线模式与流量日志。这一节的目的是让"它到底管住了什么"可核对，包括**没管住的地方**。

### 8.1 三个决定，以及它们的理由

| 决定 | 取值 |
| --- | --- |
| 出站策略 | 「默认放行并记录」/「默认询问」/「禁止出站」 |
| 离线模式 | 独立开关，默认关 |
| 日志上限 | 500 条环形缓冲，**进程内**，重启即清空 |

- **默认是"放行 + 记录"，不是"拦截"。** 第一版出站管控要解决的是可观测性：用户得先看得见这软件在连什么，才有依据决定要不要收紧。
- **离线模式是独立开关，优先于策略，且互不覆盖。** 它不是"策略的第四档"——做成第四档的话，用户改回默认档时会顺手把离线一起解除，那不是他按这个开关时想要的事。
- **未知的策略取值退回"放行"，而不是"拒绝"。** 一个损坏的设置不该让应用失去联网能力；失效方向应当由用户显式选择。
- **「默认询问」的三条取值现在都有真实行为。** 这一档曾经在界面上被禁用并给出原因（"询问需要一条请求级往返，属于独立的一批"）。它已实现，见 §8.8；档位表里 `available` 现在必须为真，而 Rust 侧有一条断言锁住"宣称可用的档位都必须真的有事发生"。

### 8.2 判定点在后端，前端不重复判

请求由 Rust 发起，因此判定也在 Rust：`modules/net/policy.rs` 的 `decide(mode, offline, loopback)` 是一个纯函数，也是唯一需要被断言的东西。前端只负责读值、写值、显示——**档位的 label / hint / 是否可用也由后端提供**（`net_policy_modes`），不手写第二份。同一份名单的第二份副本必然漂移，这与权限注册表是同一个理由。

### 8.3 收口：为什么是 `NetClient`，而不是"每个调用点记得判一次"

第一版就是后者。结果是 `plugin_http_request` 判了，而**同一个文件里的 `fetch_once` 没判**——而 `fetch_once` 正是插件市场拉索引 / 说明 / 插件包的那条路。用户把出站设成「禁止出站」、打开离线模式，刷新后市场照样能加载。

问题不在于谁忘了写，而在于**那种写法要求每个新调用点都记得写**。现在判定与记录被收进 `NetClient`：裸 `reqwest::Client` 是它的私有字段，没有任何方法把它交出去，要发请求就必须过这一层。绕不过去不是靠自律，而是靠类型。

这条纪律还有一道静态门禁（`pnpm check:network`）：`reqwest::Client` 的持有者必须恰好是门面，且 `.send()` 不允许出现在任何地方——包括门面自己（它用的是 `Client::execute`）。新加的代码一旦自己建客户端或直接发送，检查会红。

### 8.4 回环地址不受管控（含离线模式）

`127.0.0.1` / `localhost` / `::1` **在任何档位下都放行**，离线模式也是。理由：这一层要挡住的是"数据离开这台机器"，连回环地址不构成离开——它不消耗流量，也不把数据交给第三方。挡掉它的代价是两个正当场景同时失效：插件连本地开发服务器，以及网络诊断的自检。

**代价要说清楚**：如果你配置的下载源或代理指向本机（例如自建的 `http://127.0.0.1:8080/` 加速服务），那么流量会经由它出去，而出站策略与离线模式都不会拦——从应用的视角看，它只是连了本机。**要真正断网，需要同时关掉本机代理。**

### 8.5 没覆盖到的地方

- **应用更新**。检查与下载由 `tauri-plugin-updater` 自带的 HTTP 传输完成，不经过 `NetClient`，因此**不会出现在流量日志里**。覆盖到的是命令入口：离线或禁止出站时，`check_app_update` / `install_app_update` 会在发起前直接拒绝并说明原因。这是入口拦截，不是传输层拦截。
- **门面自己挡不住刻意绕行**。见 §8.6：它是可读性层，不是安全边界。
- **流量日志是诊断，不是审计**。它在内存里、有上限、重启即清空，界面上也是这么写的。URL 默认折叠查询串——那里面经常带令牌，而日志会被截图、贴进 issue。日志里的**来源字段由前端给出**，因此不能作为追责依据。

### 8.6 WebView 侧：插件不能自己联网

插件联网的**唯一合法通道是宿主的 `ctx.http`**。它走后端 `plugin_http_request`，因此带三样东西：权限检查（哪个插件、有没有声明 `network` / `network-external`）、出站策略与离线模式、以及流量日志。

而一次直接的 `fetch('https://…')` 把这三样一起绕过去了——它连"是谁发的"都无从知道。所以这一层要做的不是"给直接请求套上策略"，而是**让直接请求发不出去**。

两层，分工不同：

| 层 | 位置 | 作用 |
| --- | --- | --- |
| CSP `connect-src` | `src-tauri/tauri.conf.json` | **真正的边界**。由浏览器引擎执行，JS 改不掉。`connect-src` 只留 IPC 与回环 |
| 前端门面 | `src/services/netGuard.ts` | **可读性与可观测性**。包装 `fetch` / `XMLHttpRequest` / `WebSocket` / `sendBeacon`，给出"是谁、要去哪、为什么"并落进流量日志 |

**为什么两层都要**：CSP 挡下来的是一个静默的 `TypeError: Failed to fetch`，用户与插件作者都看不出原因；门面把它变成一句能读的话，还留下一条日志。反过来，门面只包装了四个全局函数，同一个 realm 里还有 `<img src>`、`<script src>`、`import()`、`EventSource` 等一堆不经过它们的路径——**那些只有 CSP 挡得住**。所以门面**不是安全边界**，它的价值在可观测性。

一个细节：`connect-src` 里放行了回环地址，门面也放行回环。理由是同一个——回环不出本机，而"插件连本地开发服务器"是正当场景。两处口径必须一致，否则会出现"门面放行、CSP 挡下"这种最难解释的组合。

判定规则在前端有一份**同步镜像**（`netGuard.ts` → `netControl.ts` 的常量）。理由是硬性的：`XMLHttpRequest.send()` 与 `new WebSocket()` 都是同步的，等不了一次 IPC 往返。这是全项目唯一允许存在的第二份规则，由 `pnpm check:network` 钉住——它逐个比对两侧常量，并用**同一张输入表**覆盖回环判定。

### 8.7 CSP 的人工验证清单

CSP 是这份文档里**唯一无法由自动化验证**的改动：它的失败模式是运行期的，而构建、类型检查与全部 check 脚本都不会因此变红。

改过 `tauri.conf.json` 的 `security.csp` 之后，必须在**打包版本**上逐项确认：

1. **应用能启动，且所有 `invoke` 正常** —— 这是最容易踩的坑：`connect-src` 一旦列了值就不再回退到 `default-src`，漏掉 `ipc:` / `http://ipc.localhost` 会让所有 IPC 失败，症状是启动即白屏或一片空白。
2. **插件能加载** —— `script-src` 必须留 `'unsafe-inline'`：插件是由宿主在运行期注入的 `<script>`，拿不到 Tauri 在编译期追加的 nonce/hash。同时 `dangerousDisableAssetCspModification` 里钉死了 `script-src` 与 `style-src`，防止 Tauri 给它们加上 nonce 而使 `'unsafe-inline'` 失效。
3. **插件样式生效** —— 插件样式同样是运行期注入的 `<style>`，理由同上。
4. **提示音能响，含自定义音效** —— 自定义音效经 `new Audio(url)` 播放，走 `media-src`。如果它用的不是 `data:` / `blob:`，这一条会沉默地失败。
5. **模块与插件图标正常** —— 走 `img-src`。
6. **开发模式下 HMR 可用**（`pnpm dev`）—— 走 `connect-src` 里的 `ws://localhost:*`。
7. **`ctx.http` 仍然能用** —— 它是插件联网的唯一合法通道，走 IPC，因此受第 1 条保护。
8. **插件里的一次直接 `fetch` 被拒绝** —— 这是本次改动的目的。devtools 里应当看到门面给出的话，而不是引擎的 `Failed to fetch`；「设置 → 网络 → 流量日志」里应当多出一条 `插件前端请求 / 已拒绝`。

1 至 5 中任何一条失败，都说明那条指令需要放宽——放宽时优先只动相关的那一条，并在本清单里补一句为什么。

### 8.8 「默认询问」这一档

判定函数在这一档返回的是一个**中间态**（`AllowPendingPrompt`）而不是放行或拒绝：放行等于替用户点了"允许"，拒绝则会让选了这一档的用户发现所有网络都坏了 —— 而他选的不是"禁止"。

链路（实现在 `modules/net/prompt.rs`）：

```
client.execute()
  → policy::decide() 得到 AllowPendingPrompt
  → 本会话已放行过这个主机？ ── 是 → 放行，日志记 allowed-session
                            └─ 否 → prompt::ask()
                                     · 拿队列锁（一次只弹一个，其余排队）
                                     · 拿到锁后再查一次会话放行（排队期间用户可能刚点过"总是允许"）
                                     · 发事件 net://prompt 给界面，挂起请求
                                     · 等 30 秒，或等到 net_answer_prompt
  → 允许 → 放行，日志记 allowed-by-prompt
     拒绝 / 超时 → 拒绝，日志记 denied + 原因
```

四个要点：

| 点 | 说明 |
| --- | --- |
| **超时按拒绝处理** | 放行是一次没有发生过的同意。用户离开电脑时那批请求会失败，而失败是可见、可重试的 |
| **「本会话内总是允许此主机」** | 市场一次操作会连发索引、签名、说明、图标与包下载五条请求。没有它，这一档没法用。**只作用于本次运行**，重启即失效；已放行的主机会列在设置页并可一键撤销 |
| **一次只弹一个** | 五个几乎相同的对话框互相盖住，比排一个队更糟。排队的那几条在拿到许可后会再查一次会话放行 |
| **界面挂在 `BootGate` 外面** | 出站请求会发生在启动阶段（自动检查更新）与解锁界面上，那时 `Home` 还没挂载。挂在里面不是"看不见"，而是那批请求**无人可问、只能超时** |

日志里因此多了两个取值：`allowed-by-prompt` 与 `allowed-session`。**它们不能合并成 `allowed`** —— 那会让用户再也看不出自己到底同意过什么，而"我同意过什么"正是这一档存在的全部意义。

**这一档不改变 §8.6 的边界。** WebView 里的直接 `fetch` 在 ask 档下仍然一律拒绝，理由仍然是"走错了通道"而不是"没被允许"。因此这一档只覆盖走后端门面的请求。

**没有"拒绝并记住"**：拒绝的语义是"这次不行"，把它记成长期规则会让"我总是拒绝它"和"我这次不想"混为一谈，而后者更常见。

## 9. 已知限制

- **不支持 HTTP/SOCKS 代理。** 这里实现的是「地址改写」而不是「流量转发」，因此系统级代理、需要认证的代理都不在覆盖范围内。`UpdaterBuilder::proxy()`（真正的 HTTP 代理）没有被使用。
- **只支持前缀式加速源。** 少数服务使用别的形式（例如把域名替换掉、或需要特定的路径前缀），本实现不覆盖。
- **`@tauri-apps/plugin-updater` 仍留在 `package.json` 的依赖里但没有被使用**，因为删除依赖需要重新生成 `pnpm-lock.yaml`。它的权限（`updater:default`）已经移除，因此即使有人重新 import，调用也会因权限不足而失败。
- **内置的加速源预设只是输入建议**，不是「官方支持列表」。它们是第三方服务，可用性、速度与可信度由它们自己负责。
