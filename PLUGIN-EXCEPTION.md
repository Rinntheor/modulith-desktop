# Modulith Desktop 插件例外（Plugin Exception）

> **一句话：框架是 GPL-3.0，只调用公开插件 API 的插件，授权由插件作者自己决定。**
>
> 本文是 GPL-3.0 第 7 条（Additional Terms）所允许的一项**额外许可**。
> 它只**放宽**插件作者的义务，不改变框架本身的 GPL-3.0 授权。

---

## 1. 这项例外解决什么问题

Modulith Desktop 以 GPL-3.0 发布。GPL-3.0 的 copyleft 约束的是**衍生作品**：如果你分发基于本框架的衍生作品，那么该作品也必须以 GPL-3.0 授权并提供源代码。

这对框架本身是刻意的选择 —— 防止有人拿这个框架做**闭源商业产品**。

但插件处在另一件事上。本框架的插件与宿主：

- 运行在**同一个 WebView、同一个 JavaScript 上下文**中；
- 复用**宿主的 React 实例**（`Modulith.React`），不能自带 React 副本；
- 通过 `Modulith.registerModule()` 把组件**交给宿主渲染**；
- 可以调用宿主暴露的 Tauri 命令。

这些特征使插件在法律上**有可能**被认定为框架的衍生作品。若如此，插件作者将在事实上被迫以 GPL-3.0 授权自己的作品 —— 包括那些本可以自由选择许可（MIT、Apache-2.0、专有）的插件。

**这项例外就是为了消除这个不确定性**：明确授予插件作者自行选择授权方式的权利。

---

## 2. 授予的额外许可

作为 Modulith Desktop 的版权持有人，我们额外许可：

> **任何仅通过公开插件接口与 Modulith Desktop 交互的作品（下称「插件」），
> 不因该交互而被视为 Modulith Desktop 的衍生作品，亦不受 GPL-3.0 的 copyleft
> 义务约束。插件作者可自行选择其插件的授权方式，包括专有许可。**

「公开插件接口」指：

| 接口 | 位置 |
| --- | --- |
| `window.Modulith` 宿主对象 | `Modulith.registerModule`、`Modulith.createContext`、`Modulith.React`、`jsx` / `jsxs` / `Fragment` 等成员 |
| 插件上下文服务 | `createContext()` 返回的 `storage`、`http`、`logger`、`notifications`、`events` |
| 插件清单（`manifest.json`） | 其字段与语义 |
| 宿主暴露的 Tauri 命令 | `src-tauri/src/modules/*/commands.rs` 中被注册的命令 |
| 公开的类型定义 | `src/types/plugin.ts` 等以类型声明为目的的文件 |

---

## 3. 例外的边界（请务必读完）

**这项例外只覆盖插件作者自己创作的代码。它不覆盖框架自身的源代码。**

| 行为 | 是否被例外覆盖 |
| --- | --- |
| ✅ 调用 `Modulith.*` 等公开接口 | 覆盖，可自由授权 |
| ✅ 导入公开类型定义 | 覆盖，可自由授权 |
| ✅ 以 `samples/` 下的示例为起点编写插件 | 覆盖（见第 4 节） |
| ✅ 打包时把宿主的 React 映射为外部依赖（`Modulith.React`） | 覆盖 |
| ❌ **复制或修改框架的实现代码**并闭源分发 | **不覆盖**，该部分仍受 GPL-3.0 约束 |
| ❌ 修改框架本身后分发 | 不覆盖，必须遵循 GPL-3.0 |
| ❌ 把框架代码内联进插件以规避 GPL | 不覆盖 |

换句话说：**你可以自由地为自己的插件选择许可，但不能把框架的代码搬走再闭源。**

如果你希望以非 GPL 的方式使用框架的**实现代码**（例如把 `pluginRuntime.ts` 集成进另一个产品），那需要另行取得授权 —— 请联系仓库维护者。

---

## 4. 示例代码的授权

`samples/` 目录下的示例插件，以 **MIT 许可**提供（而非 GPL-3.0）。

理由很实际：示例的用途就是**被复制**。如果示例是 GPL-3.0，那么每一个照着示例起步的新插件都会从「复制 GPL 代码」开始，其作者将陷入不必要的授权纠葛 —— 这恰好是这项例外想要避免的情况。

以示例为起点时，请把示例中的名称、作者、`icon` 等替换为你自己的信息。

---

## 5. 插件作者需要自行注意的事项

这项例外解决的是**你与框架之间**的授权关系。以下事项仍由你自行负责：

1. **你所使用的第三方库**的授权。例外不会让 GPL 库变成可用 —— 如果你在插件里引入一个 GPL 库，那个库的 GPL 义务仍然适用。
2. **你分发插件时的合规**。若你选择以 GPL 兼容许可发布，请随附相应许可证文本。
3. **不要暗示官方背书**。除非获得明确许可，请勿声称你的插件由 Modulith Desktop 官方开发、认可或支持。使用「Modulith」名称描述兼容性是允许的（例如「适用于 Modulith Desktop 的插件」），但不应使他人误认为官方出品。
4. **引擎兼容性声明**。请在 `manifest.json` 的 `engines.loopcore` 中如实声明你所兼容的宿主版本范围。注意该字段当前**不阻止安装**，版本不匹配只产生提示（见[清单文件参考](docs/02-开发指南/插件开发/清单文件参考.md)）。

---

## 6. 一项诚实的技术说明

例外条款是**授予额外许可**：版权持有人承诺不行使 GPL 赋予他的某些权利。这是有效且业界通行的做法 —— OpenJDK 的 [Classpath Exception](https://openjdk.org/legal/gplv2+ce.html) 用同一机制运作了二十余年。

但需要说清楚：**「插件是否属于衍生作品」最终由版权法与司法机关认定，不由本文声明决定。** 本文的作用是：即使某个插件在法律上被认定为衍生作品，我们也**额外许可**它以任意方式授权。

因此对插件作者而言，实际效果是确定的：**你不必因为使用本框架而改变自己插件的授权方式。**

---

## 7. 相关文档

- 插件系统架构与隔离边界：[插件系统架构](docs/02-开发指南/插件开发/插件系统架构.md)
- 清单字段（含 `license`）：[清单文件参考](docs/02-开发指南/插件开发/清单文件参考.md)
- 宿主接口签名：[宿主 API 参考](docs/02-开发指南/插件开发/宿主API参考.md)
- 版权与授权说明（中文）：[版权与授权](docs/07-法务/版权与授权.md)

---

**版权持有人：** 环理论（Rinntheor）
**项目：** Modulith Desktop
**许可证：** GPL-3.0-only，附本插件例外
