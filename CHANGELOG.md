# Changelog

本文件由 `pnpm ver bump` 依据 git 提交自动维护。
格式参考 [Keep a Changelog](https://keepachangelog.com/)。

## [1.3.2] - 2026-09-21

### Added

- 重写仪表盘，并加入用户自定义的模块分类
- 实现出站策略的「默认询问」
- 默认提示音改为随应用发布的音效（Pixabay / Universfield）

### Changed

- 版本 1.3.2
- Merge branch 'feat/plugin-api-types-guard'（插件 API 类型包与能力表的一致性门禁，及文档同步）

### Fixed

- 市场页去掉一处面向开发者的来源标注
- 救活脚本类型检查，新增 check:scripts，并修掉一条恒真断言
- 补齐彩色半透明档位的深色映射，并把 check:theme 扩到全颜色族

### Documentation

- 同步 v1.3.2 的参考文档与两处门禁盲区的记录
- 移除《插件生态设计》并同步它引出的全部引用
- 同步插件多文件源码链路，并记录 7.36 技术债

### Tests

- 插件 API 类型包与能力表的一致性门禁

## [1.3.1] - 2026-09-20

### Changed

- **插件列表不再读取 README、也不再递归计算体积。** 500 个插件的实测：列出插件一次从约 246 ms 降到约 46 ms（省下 81%），另外每次少传 15 MB 的 README 文本过 IPC。而这条路在每次启停插件、每次进插件页、每次重载插件运行时都会走
- 体积改为安装时算一次存进 `registry.json`。旧注册表由启动时做**一次性**补齐，且只在真的缺字段时写盘；**开发链接的插件仍然实时算**（源目录随时在变，缓存只会显示过期数字）

### Added

- `read_plugin_readme` 命令：README 改为按需读取 —— 全项目唯一的使用点是插件详情抽屉，而列表页一个字节都用不到它
- 规模基线测试（`cargo test --lib scale_baseline`，规模由 `SCALE_PLUGINS` 控制）。它**不断言耗时**，只打印：时间是机器相关的，把耗时变成断言只会制造一个必然在别人机器上随机失败的测试

### Fixed

- `check:markdown` 里那条「发布说明必须指明纯文本版位置」的断言。它要求出现的是 `release/notes.md` 这个路径，而那个路径只存在于一段**发布时必须删掉**的内部注记里 —— 一条在正确使用下必然失败的断言，等于没有断言，还会教人忽略红灯。换成「两份说明的首行必须指同一个版本」，那才是"拿错文件"的实际后果

### Documentation

- 性能与内存新增「插件规模」一节：优化前后的对比、做了什么，以及一处如实标注的空白 —— 前端 500 个条目的侧边栏渲染**没有**自动化覆盖，它需要真实浏览器

## [1.3.0] - 2026-09-20

> **本版本重新发布过。** 首次发布之后又合并了出站管控与流量日志、WebView 出站门面
> 与 CSP、以及插件图标在目录重建后丢失的修复，并用**同一个版本号**重新构建发行
> —— 因此最终产物比下面列出的多。代价如实记在这里：已装 1.3.0 的用户不会收到这次
> 更新的提示（版本号没变），需要手动重装。完整内容见该版本的 Release notes，
> 细节见「已知问题与技术债」7.32–7.35。

### Added

- 插件市场按**形态**分级：界面型 / 功能型 / 形态未知。带数量筛选、分组小标题，以及卡片上的贡献种类徽章（模块 / 命令 / 设置 / 右键菜单 / 后台）
- 插件管理页拆出形态筛选，并把「形态（由清单派生）」与「分类（作者填写）」分开标注来源
- `src/services/pluginShape.ts`：形态派生层。三个来源各自标明可信度 —— 清单（权威）、索引（打包时派生）、运行期实测
- `scripts/check-contributions.ts` 增 22 项形态派生断言，含「形态派生不读作者自填的 categories」这条判据

### Changed

- 索引格式新增**可选**的 `kinds` / `background`（版本级，位置与 `permissions` 一致），由打包脚本从清单派生。不动 `SCHEMA_VERSION` —— 纯新增字段，而宿主会拒绝比自己新的索引格式
- 市场里**已安装**的插件改用宿主派生的形态（权威），未安装的才回退到索引
- 插件仓库的 9 个插件迁移为声明式贡献，改为**按需激活**：不再在应用可用之后被逐个执行

### Documentation

- 快速开始新增「没有界面的插件」：四种入口骨架、后台型为什么必须登记收尾、一个纯命令插件的最小骨架
- 插件仓库的审核清单与目录规范按 1.2.0 的形态事实校正（此前那句「顶层同步调用 `registerModule()`」是已经被换掉的旧判据）

## [1.2.1] - 2026-09-20

> 本版本**未发行**：它只含测试基础设施、一处内部拆分与文档，没有任何用户可见的变化。
> 版本号与 tag 存在，是为了让这批改动在历史里有明确的归属。

### Added

- `scripts/check-plugin-runtime.ts`：插件运行时稳定性夹具。把真实的 `pluginRuntime` 装进 Node，配一套手写浏览器垫片与桩后端，用 8 个合成插件验证「未激活时不执行代码」「按需激活」「卸载时收尾」「模块 ID 抢占被拒」等机制，共 60 项断言

### Changed

- `ModuleRuntimeContext` 拆出无 JSX 的 `moduleRuntime`，使插件运行时的依赖图可以在没有构建步骤的环境里被加载

### Documentation

- 记录夹具的设计、手写垫片的代价与未覆盖面（已知问题 7.31），并订正 7.30 里「稳定性夹具还没有」那一条

## [1.2.0] - 2026-09-20

### Added

- 插件贡献模型：`contributes` 与 `activationEvents` 从「会被解析、没有消费方」变成宿主真正消费，支持模块 / 命令 / 设置 / 右键菜单四类贡献
- 设置 →「插件设置」分页：渲染插件通过清单声明的设置项，零代码贡献
- 外壳右键菜单支持插件贡献的条目
- `ctx.disposables` 与 `Modulith.onDeactivate()`：插件被禁用 / 卸载时的收尾
- `Modulith.capabilities`：宿主能力表，供插件做特性探测
- `ctx.activationEvent`：插件可以知道自己这次是为什么被激活
- `scripts/check-contributions.ts`：贡献模型的 90 项不变量断言，含对真实插件清单的归类回归

### Changed

- 插件加载拆成两趟：先由清单建立完整的界面目录（不执行任何插件代码），再按装载契约执行代码
- 加载成功的判据由「至少注册一个模块」改为「至少贡献一样东西」
- 声明式模块的 SVG 图标改为后台错峰读取，避免「插件数 × 一次 IPC」的启动尖峰

### Fixed

- 「功能性插件」被判为加载失败 —— 只加命令、只做后台监听的插件此前无法存在
- 禁用再启用插件会让命令面板丢掉清单里声明的命令条目

### Documentation

- 按代码事实更新清单文件参考、插件系统架构、已知问题与技术债、插件生态设计

## [1.1.8] - 2026-09-20

### Added

- 插件说明按 Markdown 渲染

### Fixed

- 发行版里毛玻璃开关失效 —— 只在打包后复现
- 修掉 private_interfaces 警告

### Documentation

- 同步文档，并记录本轮改动与一处未处理的注入面

## [1.1.7] - 2026-09-20

### Added

- 毛玻璃开关、通知提示音、备份机制，并修掉一批深色模式与外壳缺陷

### Documentation

- 按代码事实同步文档，并记录本轮改动

## [1.1.6] - 2026-09-19

### Added

- 窗口内分屏、全屏与开机自启动，并修正窗口状态的恢复开关

## [1.1.5] - 2026-09-19

### Changed

- Merge branch 'docs/factual-sync-1.1.4'（按代码事实校正文档）

### Fixed

- 插件命令的授权与路径收口，并校正过时文档

### Documentation

- 按代码事实校正文档，并补上遗漏的联网/日志/性能三篇索引