# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Generated files
#
# src/generated/ 完全由构建期生成，不入库。
# 它由 `pnpm gen:modules` 产出，而 `pnpm dev` / `pnpm build` 都会先跑这一步
# （见 package.json 的 scripts）。因此全新 clone 无需该目录存在。
#
# 注意：不要为它留 .gitkeep 之类的占位例外 —— 目录不需要入库，
# 生成器会自行创建它（scripts/generate-modules.ts 里有 mkdirSync）。
src/generated/

# TypeScript 增量编译缓存，属构建产物
*.tsbuildinfo

# 发布产物 staging 目录
#
# `scripts/generate-latest-json.ts` 把更新清单写到 release/latest.json，随后它连同安装包
# 一起上传到 GitHub Release。它是发布产物而不是源码：入库只会带来"提交了却没人更新"的
# 陈旧副本，而真正的更新源是那次 Release 上的那一份。
release/